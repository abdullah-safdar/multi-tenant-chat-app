apiVersion: v1
kind: Service
metadata:
  name: ai-service
  namespace: microservices
spec:
  selector:
    app: ai-service
  ports:
    - port: 3003
      targetPort: 3003
      protocol: TCP
  type: ClusterIP
